# تقرير فحص وخطة تنفيذ منظومة ERP والأنظمة الإدارية

## 1. خط الأساس المفحوص

- المرجع الأعلى: `Martyrs_Foundation_Master_Enterprise_Blueprint_v2_Final.md`، ولا سيما الأقسام 17–19 و23 و25 و30 و40–44 و50 و57 و65–66.
- التطبيق القائم: React/Vinext داخل Cloudflare Worker، واجهة عربية RTL مع تبديل عربي/إنجليزي، ونظام تصميم موحد في `app/globals.css`.
- التوجيه: مسارات داخلية يديرها `app/platform.tsx` من دون اعتماد توجيه إضافي.
- الحالة: `DemoState` typed state تُحفظ في D1 من خلال `/api/poc-state` مع fallback محلي، والمرفقات في R2.
- الموجود الذي يجب توسيعه لا تكراره: مدفوعات المستفيدين، الإيرادات وتوزيع 70/20/10، التسوية، قرارات اللجنة، الإشعارات، سجل تدقيق الحالة، لوحات المدير والإدارة العليا، والامتثال.
- الموجود حالياً في القسمين 18 و19: كتالوجات ومؤشرات قابلة للبحث والتصفية، لكنها read-only في معظمها ولا تحقق عقد الشاشة التشغيلي.

## 2. الفجوات

1. لا يوجد نموذج بيانات تشغيلي موحد للصناديق A–D أو خطوط الموازنة أو السجلات المالية/الإدارية.
2. لا توجد دورة موحدة `إنشاء ← تقديم ← اعتماد/رفض ← ترحيل/تسوية` للموديولات الـ17 المالية والـ18 الإدارية.
3. لا توجد صلاحيات تشغيلية للأدوار الداخلية التسعة ولا فصل واجبات يمنع المنشئ من الاعتماد والمعتمد من الصرف.
4. بطاقات الموديولات لا تفتح قائمة/تفصيل/نموذج/سجل حركة كامل.
5. لا يوجد صندوق مهام واعتمادات مرتبط بالدور التشغيلي.
6. لا يوجد تحقق برمجي من توازن القيد، رصيد خط الموازنة، نسبة الصندوق، أو idempotency على سجلات ERP.
7. إجراءات ERP/HR لا تكتب حالياً في سجل التدقيق والإشعارات الموحدين.
8. صفحة الأنظمة الإدارية تضم 20 عنصراً وفق الـBlueprint؛ المطلوب الحالي يركز على الـ18 الإدارية الأساسية، مع إبقاء الدعم الداخلي وITSM كتوسعة قائمة.

## 3. قرارات التنفيذ لهذا الـPOC

- إضافة طبقة مجال واحدة قابلة لإعادة الاستخدام، لا نسخ 35 شاشة متباعدة.
- كل موديول يلتزم بعقد شاشة واحد: قائمة + بحث/تصفية/فرز، تفصيل، نموذج محقق، إجراءات فعلية، حالة وسجل حركة.
- الحفاظ على D1 الحالي كمخزن durable للـPOC عبر `DemoState`؛ لا تغيير مخطط SQLite مطلوب لأن الحالة تحفظ كوثيقة JSON واحدة. هذا ليس بديلاً عن نموذج قواعد بيانات إنتاجي.
- إضافة `operationalRole` داخل مساحة العمل كتمثيل RBAC تجريبي. المصادقة والـRBAC الإنتاجيان خارج هذا الإثبات ويحتاجان IAM وسياسات خادم مستقلة.
- ربط كل إجراء بسجل التدقيق الحالي وإشعار موحد، ووضع شارة واضحة بأن البيانات والإجراءات محاكاة.
- فصل الصناديق A–D، وربط السجلات المالية بخط موازنة، وفحص القيود المزدوجة قبل الترحيل.

## 4. مراحل التنفيذ

### المرحلة 1 — الأساس المالي

- تعريف الصناديق، خطوط الموازنة، القيود المزدوجة، السجلات، الأدوار، الصلاحيات، والمهام.
- اختبارات التوازن، الرصيد، idempotency وفصل الواجبات.

### المرحلة 2 — الدورة النقدية وسلسلة الإمداد

- تفعيل AP/AR والنقد والبنوك والمطابقة، وربط المدفوعات والإيرادات القائمة.
- تفعيل المشتريات والموردين والعقود والمخزون والأصول من خلال عقد الشاشة الموحد.

### المرحلة 3 — المشاريع والمنح والتدقيق

- ربط المنحة بمرجع قرار لجنة، والدفع بخط الموازنة والصندوق C، والترحيل بقيود متوازنة.
- إظهار الأثر في التقرير المالي وسجل التدقيق.

### المرحلة 4 — نواة HR وعمليات الموظف

- تفعيل الهيكل والملفات والحضور والإجازات والتدريب والتقييم والمهام والمشاريع والأصول والمركبات والصيانة والمرافق.
- دورة إجازة قابلة للعرض: موظف يقدّم، مدير يعتمد، HR يعالج ويحدّث الرصيد.

### المرحلة 5 — الرواتب والترحيل

- تفعيل مسير رواتب قابل للإنشاء والاعتماد والترحيل إلى GL/AP مع فصل الواجبات.

### المرحلة 6 — الربط والقبول

- مكاتب أدوار وصندوق مهام/اعتمادات.
- تحديث RTM والعمارة والنطاق والافتراضات.
- اختبارات domain وinteraction وtypecheck وlint وbuild، ثم نشر النسخة.

## 5. الملفات المخطط تعديلها

- `lib/types.ts`: عقود الحالة والكيانات التشغيلية.
- `lib/enterprise.ts`: الكتالوج، RBAC/SoD، التحقق، وانتقالات سير العمل.
- `lib/seed.ts`: بيانات POC مترابطة لكل الموديولات وقصتي المنحة والإجازة.
- `app/enterprise-operations.tsx`: مكتب الدور، صندوق المهام، وعقد الشاشة الموحد.
- `/staff/erp`: **بوابة الموظف المؤسسية** بصلاحية موظف ثابتة ونماذج مختلفة حسب نوع الخدمة؛ الطلب يحمل صاحبه ومصدره وينتقل إلى `/manager/employee-requests` ثم إلى الجهة الإدارية المختصة، من دون كشف أدوات الإدارة أو المحاسبة للموظف.
- `app/platform.tsx`: التنقل، المسارات، والربط بالحالة والتدقيق والإشعارات.
- `app/globals.css`: أنماط المكاتب، القوائم، النماذج، وتجاوب الهاتف.
- `tests/domain.test.ts` و`tests/interaction.test.ts`: معايير القبول القابلة للأتمتة.
- `ARCHITECTURE.md`, `POC_SCOPE.md`, `REQUIREMENTS_TRACEABILITY.md`, `ASSUMPTIONS.md`, `README.md`: توثيق التنفيذ وحدوده.

## 6. معيار الإكمال لهذه الدفعة

- جميع الموديولات المالية الـ17 والإدارية الـ18 موصولة بقائمة تشغيلية واحدة قابلة لإعادة الاستخدام وليست عناوين فارغة.
- يمكن إكمال دورة إنشاء/تقديم/اعتماد/ترحيل عبر تبديل الدور التشغيلي، مع منع فصل الواجبات المخالف.
- كل حركة تحفظ في الحالة الدائمة للـPOC وتولد إشعاراً وأثراً تدقيقياً.
- قصة المنحة وقصة الإجازة/الرواتب قابلة للعرض، مع إفصاح واضح بأن كل ذلك محاكاة ولا يمثل نظاماً محاسبياً أو HR إنتاجياً.
